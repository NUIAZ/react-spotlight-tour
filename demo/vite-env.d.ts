/// <reference types="vite/client" />
/**
 * Pulls in Vite's ambient module declarations so `import './demo.css'` and
 * `import 'react-spotlight-tour/styles.css'` typecheck. Demo-only; the
 * published package has no CSS imports in its JavaScript at all.
 */
export {};
